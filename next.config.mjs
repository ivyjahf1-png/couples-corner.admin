/** @type {import('next').NextConfig} */
const nextConfig = {
  // Minimal config — Vercel auto-detects most settings.
  // Images: if admin/content uses next/image, add a `images` domain here.
};

export default nextConfig;
